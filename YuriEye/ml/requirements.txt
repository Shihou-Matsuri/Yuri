# YuriEye ML 路径依赖（检测/分类模型训练）
# 建议在独立 conda 环境安装（见 ml/README.md）：
#   conda create -n yurieye python=3.10
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   pip install -r ml/requirements.txt
ultralytics>=8.3.0
opencv-python>=4.8
numpy>=1.24
PyYAML>=6.0
